name: 🔄 Atualizar Feed RSS

on:
  schedule:
    # Executar a cada hora (no minuto 0)
    - cron: '0 * * * *'
    
  # Permitir execução manual
  workflow_dispatch:

jobs:
  update-feed:
    runs-on: ubuntu-latest
    
    steps:
    - name: 📥 Baixar código
      uses: actions/checkout@v3
      
    - name: 🐍 Configurar Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'
        
    - name: 📦 Instalar dependências
      run: pip install -r requirements.txt
      
    - name: 🔄 Executar atualização
      run: python update_feed.py
      
    - name: 💾 Verificar resultado
      run: |
        echo "Verificando feed.xml..."
        ls -la feed.xml
        echo "Primeiras 3 linhas:"
        head -3 feed.xml
        
    - name: 📤 Fazer commit das alterações
      run: |
        git config --global user.name 'GitHub Actions'
        git config --global user.email 'actions@github.com'
        git add feed.xml
        git diff --cached --quiet || (git commit -m "🔄 Atualização automática - $(date +'%d/%m/%Y %H:%M')" && git push)
        
    - name: ✅ Notificar sucesso
      if: success()
      run: |
        echo "🎉 Feed atualizado com sucesso!"
        echo "📅 Próxima atualização: daqui a 1 hora"
        echo "🔗 URL: https://thecrossnow.github.io/feed-leg-ftz/feed.xml"
